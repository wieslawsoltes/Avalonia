<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal">

  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="20">
        <HyperlinkButton Content="_Click Me!" />
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <Thickness x:Key="MacOS.HyperlinkButtonBorderThemeThickness">1</Thickness>

  <ControlTheme x:Key="{x:Type HyperlinkButton}" TargetType="HyperlinkButton">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Background" Value="{DynamicResource MacOS.HyperlinkButtonBackground}" />
    <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
    <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource MacOS.HyperlinkButtonBorderBrush}" />
    <Setter Property="BorderThickness" Value="{DynamicResource MacOS.HyperlinkButtonBorderThemeThickness}" />
    <Setter Property="CornerRadius" Value="{DynamicResource MacOS.ControlCornerRadius}" />
    <Setter Property="Padding" Value="{DynamicResource MacOS.ButtonPadding}" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="RenderTransform" Value="none" />
    <Setter Property="TextBlock.TextDecorations" Value="Underline" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="0:0:.075" />
      </Transitions>
    </Setter>

    <Setter Property="Template">
      <ControlTemplate>
        <ContentPresenter x:Name="PART_ContentPresenter"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}"
                          Content="{TemplateBinding Content}"
                          ContentTemplate="{TemplateBinding ContentTemplate}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
      </ControlTemplate>
    </Setter>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="RecognizesAccessKey" Value="True" />
    </Style>

    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource MacOS.HyperlinkButtonBackgroundPointerOver}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.HyperlinkButtonBorderBrushPointerOver}" />
      <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundPointerOver}" />
    </Style>

    <Style Selector="^:pressed">
      <Setter Property="RenderTransform" Value="scale(0.98)" />
    </Style>

    <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource MacOS.HyperlinkButtonBackgroundPressed}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.HyperlinkButtonBorderBrushPressed}" />
      <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundPressed}" />
    </Style>

    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource MacOS.HyperlinkButtonBackgroundDisabled}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.HyperlinkButtonBorderBrushDisabled}" />
      <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundDisabled}" />
    </Style>

    <Style Selector="^:visited">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundVisited}" />
      </Style>

      <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundVisitedPointerOver}" />
      </Style>

      <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.HyperlinkButtonForegroundVisitedPressed}" />
      </Style>
    </Style>

  </ControlTheme>
</ResourceDictionary>
