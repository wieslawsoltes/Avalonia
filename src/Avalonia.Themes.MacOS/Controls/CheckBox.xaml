<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20">
      <StackPanel Spacing="20">
        <CheckBox>Unchecked</CheckBox>
        <CheckBox IsChecked="True">Checked</CheckBox>
        <CheckBox IsThreeState="True" IsChecked="{x:Null}">Indeterminate</CheckBox>
        <CheckBox Width="120">Checkbox should wrap its text</CheckBox>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <StreamGeometry x:Key="CheckMarkPathData">M5.5 10.586 1.707 6.793A1 1 0 0 0 .293 8.207l4.5 4.5a1 1 0 0 0 1.414 0l11-11A1 1 0 0 0 15.793.293L5.5 10.586Z</StreamGeometry>
  <x:Double x:Key="MacOS.CheckBoxMinHeight">32</x:Double> 
  
  <ControlTheme x:Key="{x:Type CheckBox}" TargetType="CheckBox">
    <Setter Property="Padding" Value="{DynamicResource MacOS.CheckBoxPadding}" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="CornerRadius" Value="{DynamicResource MacOS.ControlCornerRadius}" />
    <Setter Property="MinHeight" Value="{DynamicResource MacOS.CheckBoxMinHeight}" />
    <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundUnchecked}" />
    <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundUnchecked}" />
    <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushUnchecked}" />
    <Setter Property="Template">
      <ControlTemplate>
        <Grid x:Name="RootGrid" ColumnDefinitions="20,*">
          <Border x:Name="PART_Border"
                  Grid.ColumnSpan="2"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}" />

          <Grid VerticalAlignment="Top" Height="{DynamicResource MacOS.CheckBoxMinHeight}">
            <Border x:Name="NormalRectangle"
                    BorderBrush="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeUnchecked}"
                    Background="{DynamicResource MacOS.CheckBoxCheckBackgroundFillUnchecked}"
                    BorderThickness="{DynamicResource MacOS.CheckBoxBorderThemeThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    UseLayoutRounding="False"
                    Height="20"
                    Width="20" />

            <Viewbox UseLayoutRounding="False">
              <Panel>
                <Panel Height="16" Width="16" />
                <Path x:Name="CheckGlyph"
                      Opacity="0"
                      Fill="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundUnchecked}"
                      Stretch="Uniform"
                      VerticalAlignment="Center"
                      FlowDirection="LeftToRight" />
              </Panel>
            </Viewbox>
          </Grid>
          <ContentPresenter x:Name="PART_ContentPresenter"
                          ContentTemplate="{TemplateBinding ContentTemplate}"
                          Content="{TemplateBinding Content}"
                          Margin="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                          TextWrapping="Wrap"
                          Grid.Column="1" />
        </Grid>
      </ControlTemplate>
    </Setter>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="RecognizesAccessKey" Value="True" />
    </Style>

    <!-- Unchecked PointerOver State -->
    <Style Selector="^:pointerover">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundUncheckedPointerOver}" />
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundUncheckedPointerOver}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushUncheckedPointerOver}" />
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeUncheckedPointerOver}" />
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillUncheckedPointerOver}" />
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundUncheckedPointerOver}" />
      </Style>
    </Style>

    <!-- Unchecked Pressed State -->
    <Style Selector="^:pressed">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundUncheckedPressed}" />
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundUncheckedPressed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushUncheckedPressed}" />
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeUncheckedPressed}" />
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillUncheckedPressed}" />
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundUncheckedPressed}" />
      </Style>
    </Style>

    <!-- Unchecked Disabled state -->
    <Style Selector="^:disabled">
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundUncheckedDisabled}" />
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundUncheckedDisabled}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushUncheckedDisabled}" />
      </Style>

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeUncheckedDisabled}" />
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillUncheckedDisabled}" />
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundUncheckedDisabled}" />
      </Style>
    </Style>

    <Style Selector="^:checked">
      <!-- Checked Normal State -->
      <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundChecked}" />
      <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundChecked}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushChecked}" />

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillChecked}" />
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillChecked}" />
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundChecked}" />
        <Setter Property="Data" Value="{StaticResource CheckMarkPathData}" />
        <Setter Property="Width" Value="9" />
        <Setter Property="Opacity" Value="1" />
      </Style>

      <!-- Checked PointerOver State -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundCheckedPointerOver}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundCheckedPointerOver}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushCheckedPointerOver}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeCheckedPointerOver}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillCheckedPointerOver}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundCheckedPointerOver}" />
        </Style>
      </Style>

      <!-- Checked Pressed State -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundCheckedPressed}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundCheckedPressed}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushCheckedPressed}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeCheckedPressed}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillCheckedPressed}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundCheckedPressed}" />
        </Style>
      </Style>

      <!-- Checked Disabled State -->
      <Style Selector="^:disabled">
        <Style Selector="^ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundCheckedDisabled}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundCheckedDisabled}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushCheckedDisabled}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeCheckedDisabled}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillCheckedDisabled}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundCheckedDisabled}" />
        </Style>
      </Style>
    </Style>

    <Style Selector="^:indeterminate">
      <!-- Indeterminate Normal State -->
      <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundIndeterminate}" />
      <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundIndeterminate}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushIndeterminate}" />

      <Style Selector="^ /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeIndeterminate}" />
        <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillIndeterminate}" />
      </Style>

      <Style Selector="^ /template/ Path#CheckGlyph">
        <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundIndeterminate}" />
        <Setter Property="Data" Value="M1536 1536v-1024h-1024v1024h1024z" />
        <Setter Property="Width" Value="7" />
        <Setter Property="Opacity" Value="1" />
      </Style>

      <!-- Indeterminate PointerOver State -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundIndeterminatePointerOver}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundIndeterminatePointerOver}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushIndeterminatePointerOver}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeIndeterminatePointerOver}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillIndeterminatePointerOver}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundIndeterminatePointerOver}" />
        </Style>
      </Style>

      <!-- Indeterminate Pressed State -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundIndeterminatePressed}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundIndeterminatePressed}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushIndeterminatePressed}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeIndeterminatePressed}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillIndeterminatePressed}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundIndeterminatePressed}" />
        </Style>
      </Style>

      <!-- Indeterminate Disabled State -->
      <Style Selector="^:disabled">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
          <Setter Property="Foreground" Value="{DynamicResource MacOS.CheckBoxForegroundIndeterminateDisabled}" />
        </Style>

        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxBackgroundIndeterminateDisabled}" />
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxBorderBrushIndeterminateDisabled}" />
        </Style>

        <Style Selector="^ /template/ Border#NormalRectangle">
          <Setter Property="BorderBrush" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundStrokeIndeterminateDisabled}" />
          <Setter Property="Background" Value="{DynamicResource MacOS.CheckBoxCheckBackgroundFillIndeterminateDisabled}" />
        </Style>

        <Style Selector="^ /template/ Path#CheckGlyph">
          <Setter Property="Fill" Value="{DynamicResource MacOS.CheckBoxCheckGlyphForegroundIndeterminateDisabled}" />
        </Style>
      </Style>
    </Style>
  </ControlTheme>
</ResourceDictionary>
