<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:ClassModifier="internal">
  <Design.PreviewWith>
    <StackPanel Margin="20" Width="250" Spacing="24">
      <StackPanel Spacing="12" >
        <ToggleSwitch/>
        <ToggleSwitch IsChecked="True" />
        <ToggleSwitch HorizontalContentAlignment="Left"
                      Content="Header"
                      OffContent="Off"
                      OnContent="On"/>
        <ToggleSwitch Background="Yellow"/>
      </StackPanel>
    </StackPanel>
  </Design.PreviewWith>

  <Thickness x:Key="MacOS.ToggleSwitchTopHeaderMargin">0,0,0,6</Thickness>
  <GridLength x:Key="MacOS.ToggleSwitchPreContentMargin">6</GridLength>
  <GridLength x:Key="MacOS.ToggleSwitchPostContentMargin">6</GridLength>
  <x:Double x:Key="MacOS.ToggleSwitchThemeMinWidth">0</x:Double>

  <ControlTheme x:Key="{x:Type ToggleSwitch}" TargetType="ToggleSwitch">
    <Setter Property="Foreground" Value="{DynamicResource MacOS.ToggleSwitchContentForeground}" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="KnobTransitions">
      <Transitions>
          <DoubleTransition
              Easing="CubicEaseOut"
              Property="Canvas.Left"
              Duration="{DynamicResource MacOS.Motion.Switch}" />
        </Transitions>
    </Setter>
    <Setter Property="Template">
      <ControlTemplate>
        <Grid Background="{TemplateBinding Background}" RowDefinitions="Auto,*">

          <ContentPresenter
              x:Name="PART_ContentPresenter"
              Grid.Row="0"
              VerticalAlignment="Top"
              Content="{TemplateBinding Content}"
              ContentTemplate="{TemplateBinding ContentTemplate}"
              Margin="{DynamicResource MacOS.ToggleSwitchTopHeaderMargin}" />

          <Grid
              Grid.Row="1"
              MinWidth="{DynamicResource MacOS.ToggleSwitchThemeMinWidth}"
              HorizontalAlignment="Left"
              VerticalAlignment="Top">

            <Grid.RowDefinitions>
              <RowDefinition Height="{DynamicResource MacOS.ToggleSwitchPreContentMargin}" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="{DynamicResource MacOS.ToggleSwitchPostContentMargin}" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="12" MaxWidth="12" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid
                x:Name="SwitchAreaGrid"
                Grid.RowSpan="3"
                Grid.ColumnSpan="3"
                Background="{DynamicResource MacOS.ToggleSwitchContainerBackground}"
                Margin="0,5"
                TemplatedControl.IsTemplateFocusTarget="True" />

            <ContentPresenter
                x:Name="PART_OffContentPresenter"
                Grid.RowSpan="3"
                Grid.Column="2"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding OffContent}"
                ContentTemplate="{TemplateBinding OffContentTemplate}" />

            <ContentPresenter
                x:Name="PART_OnContentPresenter"
                Grid.RowSpan="3"
                Grid.Column="2"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Content="{TemplateBinding OnContent}"
                ContentTemplate="{TemplateBinding OnContentTemplate}" />

            <Border
                x:Name="OuterBorder"
                Grid.Row="1"
                Width="{DynamicResource MacOS.Switch.TrackWidth}"
                Height="{DynamicResource MacOS.Switch.TrackHeight}"
                Background="{DynamicResource MacOS.ToggleSwitchFillOff}"
                BorderBrush="{DynamicResource MacOS.ToggleSwitchStrokeOff}"
                BorderThickness="{DynamicResource MacOS.ToggleSwitchOuterBorderStrokeThickness}"
                CornerRadius="{DynamicResource MacOS.Switch.Radius}" />

            <Border
                x:Name="SwitchKnobBounds"
                Grid.Row="1"
                Width="{DynamicResource MacOS.Switch.TrackWidth}"
                Height="{DynamicResource MacOS.Switch.TrackHeight}"
                Background="{DynamicResource MacOS.ToggleSwitchFillOn}"
                BorderBrush="{DynamicResource MacOS.ToggleSwitchStrokeOn}"
                BorderThickness="{DynamicResource MacOS.ToggleSwitchOnStrokeThickness}"
                CornerRadius="{DynamicResource MacOS.Switch.Radius}" />

            <Canvas
                x:Name="PART_SwitchKnob"
                Grid.Row="1"
                Width="{DynamicResource MacOS.Switch.TrackHeight}"
                Height="{DynamicResource MacOS.Switch.TrackHeight}"
                HorizontalAlignment="Left">

              <Grid x:Name="PART_MovingKnobs" Width="{DynamicResource MacOS.Switch.TrackHeight}" Height="{DynamicResource MacOS.Switch.TrackHeight}">
                <Ellipse
                  x:Name="SwitchKnobOn"
                  Fill="{DynamicResource MacOS.ToggleSwitchKnobFillOn}"
                  Width="{DynamicResource MacOS.Switch.KnobSize}"
                  Height="{DynamicResource MacOS.Switch.KnobSize}" />
                <Ellipse
                  x:Name="SwitchKnobOff"
                  Fill="{DynamicResource MacOS.ToggleSwitchKnobFillOff}"
                  Width="{DynamicResource MacOS.Switch.KnobSize}"
                  Height="{DynamicResource MacOS.Switch.KnobSize}" />
              </Grid>
            </Canvas>
          </Grid>
        </Grid>
      </ControlTemplate>
    </Setter>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="RecognizesAccessKey" Value="True" />
    </Style>

    <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter:empty">
      <Setter Property="Margin" Value="0" />
    </Style>

    <!--  PointerOverState  -->
    <Style Selector="^:pointerover /template/ Border#OuterBorder">
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOffPointerOver}" />
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchFillOffPointerOver}" />
    </Style>

    <Style Selector="^:pointerover /template/ Ellipse#SwitchKnobOff">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOffPointerOver}" />
    </Style>

    <Style Selector="^:pointerover /template/ Ellipse#SwitchKnobOn">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOnPointerOver}" />
    </Style>

    <Style Selector="^:pointerover /template/ Border#SwitchKnobBounds">
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchFillOnPointerOver}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOnPointerOver}" />
    </Style>

    <Style Selector="^:pointerover /template/ Grid#SwitchAreaGrid">
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchContainerBackgroundPointerOver}" />
    </Style>

    <!--  PressedState  -->
    <Style Selector="^:pressed /template/ Border#OuterBorder">
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOffPressed}" />
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchFillOffPressed}" />
    </Style>

    <Style Selector="^:pressed /template/ Border#SwitchKnobBounds">
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchFillOnPressed}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOnPressed}" />
    </Style>

    <Style Selector="^:pressed /template/ Ellipse#SwitchKnobOff">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOffPressed}" />
    </Style>

    <Style Selector="^:pressed /template/ Ellipse#SwitchKnobOn">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOnPressed}" />
    </Style>

    <Style Selector="^:pressed /template/ Grid#SwitchAreaGrid">
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchContainerBackgroundPressed}" />
    </Style>

    <!--  DisabledState  -->
    <Style Selector="^:disabled">
      <Setter Property="Foreground" Value="{DynamicResource MacOS.ToggleSwitchHeaderForegroundDisabled}" />
    </Style>

    <Style Selector="^:disabled /template/ Border#OuterBorder">
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOffDisabled}" />
      <Setter Property="Background" Value="{DynamicResource ToggleSwitchFillOffDisabled}" />
    </Style>

    <Style Selector="^:disabled /template/ Ellipse#SwitchKnobOff">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOffDisabled}" />
    </Style>

    <Style Selector="^:disabled /template/ Ellipse#SwitchKnobOn">
      <Setter Property="Fill" Value="{DynamicResource MacOS.ToggleSwitchKnobFillOnDisabled}" />
    </Style>

    <Style Selector="^:disabled /template/ Border#SwitchKnobBounds">
      <Setter Property="Background" Value="{DynamicResource MacOS.ToggleSwitchFillOnDisabled}" />
      <Setter Property="BorderBrush" Value="{DynamicResource MacOS.ToggleSwitchStrokeOnDisabled}" />
    </Style>

    <!--  CheckedState  -->
    <Style Selector="^:checked /template/ Border#OuterBorder">
      <Setter Property="Opacity" Value="0" />
    </Style>

    <Style Selector="^:checked  /template/ Ellipse#SwitchKnobOff">
      <Setter Property="Opacity" Value="0" />
    </Style>

    <Style Selector="^:checked  /template/ Border#SwitchKnobBounds">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <Style Selector="^:checked /template/ Ellipse#SwitchKnobOn">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <Style Selector="^:checked /template/ ContentPresenter#PART_OffContentPresenter">
      <Setter Property="Opacity" Value="0" />
    </Style>

    <Style Selector="^:checked /template/ ContentPresenter#PART_OnContentPresenter">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <!--  UncheckedState  -->
    <Style Selector="^:unchecked /template/ Border#OuterBorder">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <Style Selector="^:unchecked /template/ Ellipse#SwitchKnobOff">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <Style Selector="^:unchecked /template/ Ellipse#SwitchKnobOn">
      <Setter Property="Opacity" Value="0" />
    </Style>

    <Style Selector="^:unchecked  /template/ Border#SwitchKnobBounds">
      <Setter Property="Opacity" Value="0" />
    </Style>

    <Style Selector="^:unchecked /template/ ContentPresenter#PART_OffContentPresenter">
      <Setter Property="Opacity" Value="1" />
    </Style>

    <Style Selector="^:unchecked /template/ ContentPresenter#PART_OnContentPresenter">
      <Setter Property="Opacity" Value="0" />
    </Style>
  </ControlTheme>
</ResourceDictionary>
